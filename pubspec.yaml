name: sendbird_chat_test
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  beamer: ^1.1.0
  flutter_chat_ui: ^1.5.3
  sendbird_sdk: ^3.1.7
  get_it: ^7.2.0
  injectable: ^1.5.0
  flutter_bloc: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  injectable_generator: ^1.5.2
  build_runner: ^2.1.5

flutter:

  uses-material-design: true
